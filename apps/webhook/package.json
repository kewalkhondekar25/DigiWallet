{
  "name": "webhook",
  "version": "1.0.0",
  "description": "webhook handler of digiwallet",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx esbuild ./src/index.ts --bundle --platform=node --outfile=dist/index.js",
    "start": "node dist/index.js",
    "dev": "nodemon src/index.ts"
  },
  "keywords": [
    "webhook",
    "nodejs",
    "express",
    "bullmq"
  ],
  "author": "Kewal Khondekar",
  "license": "ISC",
  "dependencies": {
    "@repo/db": "*",
    "@types/express": "^5.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "esbuild": "^0.24.0",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "prettier": "^3.4.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
